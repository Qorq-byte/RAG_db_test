# 桌面欢迎页验收（完整滚轮交互修订）

当前状态：已随 `08ebd21` 合并 main。图片全部离场后再下滚一次立即显示入口，不等待收敛或淡入；入口按钮不滴落。以下保留各次修订的历史验收，当前全桌面交付与测试边界见[主线摘要](2026-09-27-desktop-delivery.md)。

## 2026-09-26 按钮出现时机调整

按用户最新要求：图片实际绘制边界全部移出窗口后，下一次向下滚动立即显示“欢迎使用RAG系统”，不等待弹簧收敛、不额外淡入；不继续滚动则不会自动显示。反向滚动收起按钮，End / Esc 与减少动效仍能直接到入口。

欢迎页及启动定向回归 **23 passed in 43.64s**，覆盖图片已消失但动画尚未收敛时的立即显示、触控板微小增量、零增量不触发、停留不自动显示和导航切换。本次未重跑全量，下方 306 项为上一版全量基线。


## 历史：完整交互修订（后续入口时机见上文）

上一版只做散落→横列→圆环自动播放，提前显示欢迎按钮，且修改了参考文案；用户明确指出缺少滚轮变形、视差和图片离场。旧 301 项结果仅为旧行为的回归记录，不能证明满足原参考，本次以完整移植重新验收。

继续 PySide6 原生桌面，按用户 `前端设计/用户界面进入界面.txt` 恢复：

- 20 张原图、散落/横排/圆环弹簧过渡、悬停透视翻转及 View / Details。
- 0–600 滚动将圆环变为底部弧带；600–3000 沿弧线有界转动，图片依序向底部移出；上滚可逆。
- ±100px 鼠标横向视差、触控板精细增量、触摸滑动、键盘探索；惯性过渡和隐藏窗口停表。
- 原始浅色画布、英文标题/说明及淡入淡出、模糊/位移；去掉上版自行添加的品牌页首、页脚、宣传文案。
- 只有滚动到最后且图片稳定后才显示中央“欢迎使用RAG系统”；鼠标移动不移除入口，反向滚动可返回探索。点击后沿用后台初始化和导航切换。
- 窄窗口文字在圆环内部换行，避免与图片重叠；End / Esc 和减少动效提供直接到最后的桌面辅助操作，仍须显式点击进入。

## 历史验证证据（完整交互修订阶段）

- 桌面定向回归：37 passed in 42.90s；另补充真实触摸/悬停事件测试 1 passed。测试真实发送 QWheelEvent、QMouseEvent、QTest touch / hover，覆盖顺序离场、滚动上下界、回滚、视差、最终按钮稳定、计时器生命周期及原启动保护。
- 全项目：`.venv/Scripts/python.exe -m pytest -q`，**306 passed in 184.70s**。
- 最后的小窗口文字换行/间距调整后，相关尺寸、减少动效与触摸/悬停复测 **5 passed in 1.00s**。
- Windows 原生 Qt 全链路通过：实际播放开场→发送滚轮 600→底部弧带→继续滚动 1200→图片依次离场→再滚动 1200→等待按钮淡入→点击→隔离空合成库导航→切换检索页。
- 原生截图已查看圆环、弧带、中途离场、最后按钮，另检查 640×480、1280×800、1920×1080。截图保存在本地 `.data/welcome-preview/corrected-*.png`，不包含用户资料。
- 一次可见原生测试受到真实桌面额外鼠标输入干扰，出现收敛等待超时；动画状态日志证实目标值被额外输入改变。隔离 Qt 回归通过后，原生链路改为不抢占桌面的窗口再次通过。没有把失败记录算作通过，也未通过放宽超时隐藏问题。
- `git diff --check` 通过；用户原有 CLI 测试、导航草稿和入口参考文件 SHA256 与修订前相同。

## 交付

步骤 1 `7cd544d` 已推送 `codex/desktop-welcome`：完整参考交互、测试、规格修订。步骤 2 补充窄窗口文字适配和本次验收归档，独立提交推送。当时功能分支尚未合并；现已连同后续修正通过 `08ebd21` 合并 main。

20 张图片继续本地打包，来源及 SHA256 在 `src/ragdb/desktop/assets/welcome/sources.json`。不新增网页运行时，不修改知识库索引逻辑；本轮不交付 EXE 安装包。

# 桌面欢迎页验收

## 范围

依据用户提供的 `前端设计/用户界面进入界面.txt` 实现 PySide6 原生启动入口：20 张本地摄影卡片散落、聚成横列、展开圆环，结束后中央出现“欢迎使用RAG系统”；必须点击才进入已有导航工作台。动画约 4 秒，中央内容淡入 350ms。没有 WebEngine、React 或运行时网络图片依赖。

用户原始设计文件不修改；参考图片原地址及 SHA256 记录在 `src/ragdb/desktop/assets/welcome/sources.json`，20 张共 364,852 字节。

## 自动化与原生验证

- 组件测试：`.venv/Scripts/python.exe -m pytest tests/unit/test_welcome.py -q`，10 passed in 1.11s。
- 接入测试：欢迎页、启动与既有桌面测试，33 passed in 4.13s。覆盖按钮前置条件、动画自动结束仍需点击、跳过、键盘、减少动效、隐藏暂停、资源缺失兜底、尺寸变化、后台初始化、防重复、脱敏错误和重试、窗口关闭保护、最大化及真实空合成库导航。
- Windows 原生 Qt：`QT_QPA_PLATFORM=windows` 下运行新增欢迎页与启动测试，16 passed in 5.09s。
- 已查看原生 1280×800、640×480 和深色模式截图，检查卡片及中央按钮不被裁切；截图位于本地 `.data/welcome-preview/`，不包含用户真实资料。
- 原生自动化流程：实际播放完整动画并等待中央内容淡入结束，点击按钮，打开隔离空知识库的导航工作台，再切换检索页；通过。GUI 线程创建主窗口，初始化在工作线程，窗口切换不会退出进程。
- 冷启动：独立进程在含无效 TOML 的隔离工作目录中启动真实 `ragdb.desktop.app`，600ms 自动退出；退出码 0，没有导入 Chroma、读取该配置或创建资料目录。这验证首次出现欢迎页时不初始化 runtime。
- 全量命令：`.venv/Scripts/python.exe -m pytest -q`，**301 passed in 133.30s**；`git diff --check` 通过。
- 本地构建：`uv build --wheel --out-dir .data/welcome-build` 成功；wheel 491,817 字节，包含 20 张图片及启动模块。构建产物没有上传包仓库；本次不交付 EXE 安装包。

## 交付状态

工作分支 `codex/desktop-welcome`。设计计划 `202108c`、动画组件 `435b441`、启动接入 `74687ba` 已逐步提交推送；最终全量 **301 passed in 133.30s**，文档归档作为步骤 3 独立提交推送。当前功能分支已完成全部实现与验收，尚未合并 main。用户已有 CLI 测试修改与前端设计文件保持原样，不纳入本任务提交。

本次不修改索引逻辑；阶段 7 记录的 Chroma 微型索引读取限制继续有效。

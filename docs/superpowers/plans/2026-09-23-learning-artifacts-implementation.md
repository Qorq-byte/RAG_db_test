# 学习内容生成实施计划

## 步骤 1：产物领域模型、SQLite v2→v3 迁移与仓储

增加学习产物与来源快照模型、仓储和无损迁移；覆盖 CRUD、集合级联删除和引用快照测试。

## 步骤 2：固定模板与生成服务

实现摘要、提纲、笔记、练习题、卡片模板，复用检索与聊天模型；实现证据编号、空检索拒答、JSON 校验与原子写入。覆盖单元测试。

## 步骤 3：CLI 与文档

增加 `generate` 和 `artifact` 命令组，更新 README 与诊断说明；覆盖 CLI 流程、集合隔离和删除确认。

## 步骤 4：验收

运行全量 `uv run pytest`、CLI 帮助和状态检查；每个步骤测试、提交并推送。

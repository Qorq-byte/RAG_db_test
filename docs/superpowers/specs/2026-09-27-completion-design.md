# 剩余事项交付设计

用户授权：完成现有全部未完成事项，每完成一步上传 GitHub。沿用 PySide6、SQLite、Chroma 和现有模型设置，不改动用户资料或设计草稿。

## 分步交付与验收

1. 已完成全量复验记录上传：342项通过，提交9c2499b。
2. 索引可靠性：固定合成复现、定位读取错误、最小修正、错误不被静默吞掉；验证旧索引、元数据筛选、跨进程读取和维护安全边界。若第三方根因无法证实，必须区分应用层解决方案与上游根因，不能虚报修复。
3. 全库备份恢复：应用服务 + CLI + 桌面入口。持有现有独占门禁，备份 SQLite 一致快照与所有 Chroma 集合的逻辑记录（向量、文档、元数据、配置），包含对话、学习产物、日志和活动模型。SHA256清单校验，恢复到不存在的新目录，失败不覆盖原库。密钥和外部原文件不复制；已入库切片完整保留，原文件路径仅作来源引用。相比直接复制正在使用的HNSW文件，逻辑导出可跨版本审计且不依赖未刷盘段。
4. 检索质量评估：可复用的带标注JSON数据集、Recall@K、MRR@K、nDCG@K、逐查询结果及延迟，CLI运行和JSON报告。离线固定数据集验证计算与检索链路，支持用户库真实评估；明确合成评估与真实模型质量的边界。
5. Windows交付：PyInstaller目录包 + 用户级安装程序，打包资源和运行依赖，数据默认写用户目录，云端/本地模型服务仍由用户配置。相比单文件巨大EXE，目录包启动更快、便于诊断；安装程序统一分发。构建脚本、打包自检、启动验收与SHA256上传GitHub Release，源码逐步推送。
6. 最终全量回归与文档整理，逐项核实交付证据和仓库同步。

## 完成规则

每步针对性测试通过后提交明确文件并推送 main；不纳入原有测试文件和前端设计目录。安装包发布附构建方式、依赖服务说明和可验证校验值。任何环境或外部服务阻塞如实记录，不用重复通过代替失败证据。

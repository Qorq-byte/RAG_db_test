# Findings: 检索元数据筛选

## 当前实现

- `Source` 与 `Chunk` 已有 JSON 元数据字段。
- `SearchService` 已将同一份筛选映射传入向量与关键词检索。
- CLI 仅公开 `--source-type` 与 `--source-id`。
- SQLite FTS 目前只支持资料 ID、代次和资料类型筛选。
- ChromaDB 可存储标量筛选字段，但当前只向切片写入 `source_type` 等系统元数据；字符串标签列表不可作为稳定的 AND 筛选基础。

## 已确认的产品契约

- 支持 `tags`、`course`、`author`、`date` 四个资料级元数据字段。
- 多个搜索标签采用 AND：每个返回资料必须同时拥有所有请求标签。
- 日期格式是 ISO `YYYY-MM-DD`，范围边界包含在内。
- 元数据在导入时写入资料，并传播到切片和向量索引。

## 风险与处理

- 不应在召回后才筛选，否则固定候选池会漏掉本应命中的资料。
- 旧资料不能可靠地补出缺失元数据；仅新导入或重新导入资料可匹配新条件。

## 实施结果

- SQLite JSON 查询逐一验证每个请求标签；ChromaDB 为每个标签写入哈希派生的布尔字段，保证标签交集语义一致。
- 内容不变而元数据改变时，需要重新建立索引。SQLite 对稳定切片 ID 使用 upsert，既保留既有 ID 契约，也允许代次更新。
- 搜索 CLI 公开 `--tag`、`--course`、`--author`、`--date-from`、`--date-to`；范围边界包含在内。

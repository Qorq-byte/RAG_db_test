# 项目交付记录

## 2026-09-27 全库备份恢复

实现一致SQLite快照与Chroma逻辑导出、逐文件SHA256、只恢复到新目录、CLI三命令及桌面后台入口；桌面新增 --config 参数可打开恢复库。30项联合回归通过，补充后8项备份专项通过。详细范围及证据见[备份验收](docs/acceptance/2026-09-27-library-backup.md)。上一项索引恢复已通过2beb4f6推送。

当前交付以[进度总览](task_plan.md)及[桌面主线摘要](docs/acceptance/2026-09-27-desktop-delivery.md)为准。以下按时间倒序保留历史事实；旧条目中的“未合并”、交互方式及测试数描述当时状态，不代表当前版本。

## 2026-09-27 当前主线全量复验

后续收尾：用户授权完成所有剩余项并逐步上传。验收记录已通过9c2499b推送，实施设计通过687bac3推送。索引读取恢复证据见[专项验收](docs/acceptance/2026-09-27-index-reader-recovery.md)：最终86项定向通过，384次真实合成查询全部成功，6次通过独立读取进程恢复。应用修正不等于上游缺陷根因修复。

基于 `main` 的 `31347c5`，执行 `.venv/Scripts/python.exe -m pytest -q --basetemp .data/verification-full-20260927-b -p no:cacheprovider --junitxml .data/verification-full-20260927-b.xml`，结果 **342 passed in 314.90s (0:05:14)**，退出码0，无失败或跳过。测试报告保存在本地忽略目录 `.data/`。

初始沙箱运行在 pytest 临时目录准备阶段遇到 `PermissionError: [WinError 5]`；改用工作区临时目录仍有同类权限阻塞，停止无效运行后，经自动审批在沙箱外使用隔离目录完成全量。环境阻塞不计为产品回归。本轮未修改产品代码、依赖或用户原有测试与设计文件，未新增真实模型服务验收；未复现 Chroma 微型索引异常，但仍保留其根因调查待办。

## 2026-09-27 文档整理

统一主线合并状态为 `08ebd21`，整理按钮最终范围、主题图标与轮播交互，区分合并定向回归和历史全量结果；补齐验收索引与未完成事项。仅整理文档，未新增产品代码或测试结论。

## 2026-09-27 桌面界面修改合并主线

用户明确要求全部上传并合并。获取远端最新主线后，确认早期欢迎页已通过 PR #4 合并；将后续完整欢迎交互、侧边栏轮播、主题修复与图标、按钮参考还原及滴落范围限制（截至 `bc4b18c`）通过 `08ebd21` 一并合并 main，无冲突。合并前所有功能提交均已上传。

合并后代码与已测试功能分支一致。隔离 main 工作目录回归 **69 passed in 99.72s**，覆盖桌面、主题、模型设置、液滴与轮播。本次未重跑全量；上一轮335项通过、1项 Chroma 间歇失败及独立复测通过的记录保留。原工作区用户未提交文件未纳入。

## 2026-09-27 重新加载按钮取消滴落

按用户要求将模型设置顶部“重新加载”明确排除滴落，保留重新加载功能。模型设置页与液滴回归29项通过（8.56s）。

## 2026-09-27 滴落仅限页面顶部

按用户最新要求改为显式启用：仅页面标题操作栏和顶部“详情”按钮允许滴落；主题图标、密钥清除等内容区按钮、侧边栏及欢迎页入口均禁用。按钮、主题、桌面回归34项、模型设置页面18项通过；未重跑全量。

## 2026-09-27 按钮滴落还原与侧边栏恢复

根据用户反馈重新对照参考：恢复固定宽度圆底液柱、凹弧连接、CSS easeIn 曲线及父子统一缩放，修正尖底和落滴轨迹偏差。侧边栏整体退出液滴效果，导航及底部控件恢复原样，保留随机指示线和主题图标。原生分帧图及 8 秒预览已生成；桌面定向47项、补充后的专项14项通过。本次未重跑全量。

## 2026-09-27 液体按钮与主题图标

按 `前端设计/按钮.txt` 在 PySide6 原生实现四处错峰液体滴落；仅鼠标靠近按钮 18px 内启用，移开清除，减少动效时关闭。绘制层不拦截点击并避让相邻控件与文本；保留危险按钮红色和侧栏随机指示线。主题切换改为太阳、月亮、显示器图标，分别对应浅色、深色、跟随系统。

原生 Windows 截图检查通过，修正了兄弟控件坐标映射导致液滴不可见的问题。定向 62 项通过；修正后专项 11 项通过。全量 **335 passed、1 failed in 360.52s**；失败再现历史 Chroma 微型索引读取错误，单独复测 **1 passed in 2.38s**，未声称问题已修复。

用户确认后，实现 `7d28177` 与验收记录 `496fe82` 已成功推送至 `Qorq-byte/RAG_db_test` 的 `codex/desktop-welcome` 分支，尚未合并 main。详见 [按钮验收](docs/acceptance/2026-09-27-desktop-buttons.md)。

## 2026-09-27 连续滚动无等待切换

按用户要求取消切换期间丢弃滚轮输入的限制。新输入直接更新切换目标，并从当前绘制位置及运动速度继续过渡；支持中途反向和大幅滚动，不建立延迟翻页队列。停止输入后约 260ms 完成当前过渡。初次入场仍保留错峰动画，悬停、增删、主题和减少动效保持可用。

定向回归 **38 passed in 41.49s**。Windows 原生以 65ms 间隔连续发送 12 次下滚及 3 次上滚，逐次检查目标均已响应，停止后 500ms 内稳定为七张卡片。测试同时检查中途更新前后的实际位置、速度连续，以及反向后无残留卡片。本次未重跑全项目测试，上一版全量基线为 325 项。


## 2026-09-26 改为鼠标滚动切换

按最新要求移除左右箭头，改为图片区域滚轮下滚下一张、上滚上一张；支持精细触控板和横向滚动，保留圆点、悬停动效和键盘切换。增加“滚动切换图片”提示。动画期间输入不排队，离开或隐藏清空未完成增量。

定向回归 **36 passed in 22.50s**；Windows 原生真实 QWheelEvent 切换及悬停、移除恢复、折叠、主题、导航全流程通过。本次未重跑全量，上一版基线为 325 项通过。


## 2026-09-26 桌面主题与导航线条修复

- 最终全量 **325 passed in 202.96s**。

- 修复真实 QComboBox 返回字符串导致 set_mode 抛错的问题；主题同步更新背景、文本和原生控件调色板。跟随系统监听系统配色，旧 ThemeManager 不再覆盖当前实例的手动主题。
- 点击导航功能时从六组非蓝色配色随机选择线条颜色；同一项再次点击换色，普通重绘不换色，适配深浅背景。
- 实现 `46c582f`、实例竞争修正 `208169f` 已逐步推送。原生下拉框背景像素与截图验证通过；首次全量中的已知 Chroma 间歇性失败及主题实例问题如实记录在 [主题修复验收](docs/acceptance/2026-09-26-desktop-theme-fix.md)。

## 2026-09-26 侧边栏底部扇形轮播交付

- 按用户 `前端设计/侧边栏底部ui.txt` 原生实现十张图片、七卡扇形层叠、错峰弹性入场、悬停抬升/邻卡让位、左右循环进出场与圆点。图片离线打包。
- 底部“添加底部卡片 / 移除底部卡片”切换并保存重启偏好；折叠和过矮窗口暂时隐藏，空间恢复后显示，不改写偏好。支持深浅主题、键盘和减少动效，隐藏停止动画。
- 第一步实现和规格 `cafb250` 已推送；第二步回归及文档独立提交推送。定向 30 passed in 10.49s；**全量 321 passed in 195.81s**。Windows 原生入场→悬停→切换→移除/恢复→折叠→主题→导航流程通过；截图已查看。
- wheel 构建通过，确认原生模块和十张侧边栏图片包含在包内。用户 CLI 测试与设计文件保持原样。本分支 `codex/desktop-welcome` 尚未合并 main。

## 2026-09-26 按钮出现时机调整

按用户最新要求：图片实际绘制边界全部移出窗口后，下一次向下滚动立即显示“欢迎使用RAG系统”，不等待弹簧收敛、不额外淡入；不继续滚动则不会自动显示。反向滚动收起按钮，End / Esc 与减少动效仍能直接到入口。

欢迎页及启动定向回归 **23 passed in 43.64s**，覆盖图片已消失但动画尚未收敛时的立即显示、触控板微小增量、零增量不触发、停留不自动显示和导航切换。本次未重跑全量，下方 306 项为上一版全量基线。


## 2026-09-26 桌面欢迎页：完整交互修订

- 用户明确否定上一版省略滚轮交互的范围缩减。恢复原参考的散落/横列/圆环、滚轮变底部弧带、继续滚动顺序离场、鼠标视差、翻转、原始文案与过渡；欢迎按钮只在最后出现。
- 第一步 `7cd544d` 已提交推送。第二步检查小窗口并修正文字与圆环重叠，更新规格、计划、README 与验收记录，独立提交推送。
- 全量 **306 passed in 184.70s**；最后尺寸适配后定向 **5 passed in 1.00s**。真实 Qt wheel / touch / hover 测试通过；Windows 原生滚轮全流程到隔离导航通过，640×480 / 1280×800 / 1920×1080 截图已检查。
- 保留用户设计原文件与已有 CLI 测试修改（SHA256 不变）。当前分支 `codex/desktop-welcome`，尚未合并 main。下列旧欢迎页记录为修订前历史，不代表最新交互规格。

## 2026-09-26 桌面欢迎页：步骤 3 验收与交付

- 步骤 2 已以 `74687ba` 推送。Windows 原生新增测试 16 passed in 5.09s；自动播放完整动画、等待中央按钮淡入、点击进入真实空合成库导航并切换检索页的流程通过。
- 冷启动确认未提前读取配置、导入 Chroma 或创建资料目录；wheel 构建成功并包含全部 20 张本地图片。已检查正常/小窗口、深色及动画阶段截图。
- 最终全量 **301 passed in 133.30s**；README、详细计划和 [欢迎页验收](docs/acceptance/2026-09-26-desktop-welcome.md) 已更新。用户原有 CLI 测试、导航草稿和入口设计文件 SHA256 未变。
- 三步已完成，逐步交付 `codex/desktop-welcome`；当前尚未合并 main，不影响阶段 7 的主线归档。

## 2026-09-26 桌面欢迎页：步骤 2

- 步骤 1 已以 `435b441` 推送。`ragdb-gui` 现在先显示欢迎动画；只有点击“欢迎使用RAG系统”后才后台初始化 runtime，再进入已有导航主窗口。
- 初始化失败显示脱敏提示并允许重试；防止重复进入，初始化期间关闭受保护；进入时保留窗口尺寸与最大化状态。存储/模型相关模块延迟导入，开场不读取用户库。
- 欢迎页、启动及既有桌面定向回归 **33 passed in 4.13s**，包含真实空合成库进入后检索/设置导航、失败重试和 GUI 线程断言。

## 2026-09-26 桌面欢迎页：步骤 1

- 用户指定参考 `前端设计/用户界面进入界面.txt`，继续 PySide6 原生桌面形式；设计计划已以 `202108c` 推送 `codex/desktop-welcome`。
- 已实现 20 张本地摄影卡片的散落→横列→圆环动画、悬停翻转，结束后中央出现“欢迎使用RAG系统”。支持 Esc 跳过、键盘入口、减少动效与深浅主题。
- 组件测试 10 passed in 1.11s；已查看 Windows 原生正常/小窗口和深色截图。启动接入为下一步，当前组件不创建 runtime 或访问真实资料。

## 2026-09-26 阶段 7 主线合并完成

- 用户明确回复“合并”后，已将 `codex/index-reliability` 通过 `4651ace` 无冲突合并并推送 GitHub `main`。
- 合并树与已验收分支完全一致，沿用最终全量 285 项、Windows 原生 Qt 22 项和 doctor 通过的证据；没有产品代码变化，本轮未重复测试。
- 进度总览、README、计划和验收记录更新为完成。用户原有 CLI 测试及两份前端草稿保持不变，未纳入提交；微型索引底层读取异常的已知限制继续保留。

## 2026-09-26 阶段 7 推送完成、主线合并待授权

- 四个步骤依次推送：`fe037b9`、`69cde2b`、`3111aeb`、`39b2e45`；远端分支 `codex/index-reliability` 包含全部开发与验收。最终产品回归 285 项通过。
- 自动审批拒绝了 `git switch main; git merge --no-ff codex/index-reliability`，理由是本次用户明确要求逐步开发并推送，未明确授权修改默认分支。命令没有执行，本地和远端 main 均保持 `6eae658`。
- 不绕过拒绝；待用户明确授权后再合并。用户原有文件保持不变，合并准备已就绪。

## 2026-09-26 阶段 7.4：联合验收完成

- 7.3 已以 `3111aeb` 推送；完整流程覆盖发布后新进程检索、CLI 预览与确认清理、再次检索及 SQLite/FTS 保持。Windows 原生 Qt 22 passed，doctor 退出码 0。
- 首次全量 1 failed/284 passed：微型索引在清理预检中不可读，操作安全拒绝；只读核对 4 个新索引、4 个旧索引、切片和 FTS 均保留。成功清理流程改用默认阈值以上的 4,004 条合成切片，微型索引及故障保护用例仍保留。
- 最终全量 **285 passed in 104.43s**。README、进度、计划和 [验收记录](docs/acceptance/2026-09-26-index-reliability.md) 已同步；底层精确根因未证明解决。
- 用户原有三份文件 SHA256 未变，uv.lock 未变；全部实验未清理真实知识库。该步骤独立提交推送后按既有计划合并主线。

## 2026-09-26 阶段 7.3：发布前查询校验

- 7.2 已以 `69cde2b` 推送。所有非空候选集合真实查询并核对当前切片身份后才发布；校验期间可取消，不增加模型调用；桌面显示查询验证阶段。
- 新校验捕获底层读取异常并拒绝发布，两轮定向分别 1 failed/66 passed 与 1 failed/67 passed，未掩盖失败。固定 48 轮调查 192 次查询失败 6 次，短等待无恢复，释放全部客户端后均可读。
- 据此在重建中释放写入者后再新建读取者；固定对照 192 次查询全部通过。仍有其他所有者时不重置共享系统，若不可读则拒绝发布。
- 修改后定向 68 passed in 40.60s；共享存活者补充测试 1 passed in 2.26s。底层精确根因仍未证明，详见阶段 7 验收记录。

## 2026-09-26 阶段 7.2：客户端生命周期

- 7.1 已以 `fe037b9` 推送。向量适配器新增幂等关闭和上下文管理，runtime 每次向量操作完成或异常后关闭；重建和维护各自释放客户端。
- 统一创建/释放锁保护 Chroma 共享系统，单个所有者退出不关闭其他客户端。未更改依赖或添加读取重试。
- 定向 85 passed in 46.36s；引用回归、并发存活者、重建成功/失败/取消和进程重开通过。历史读取错误仍未归因。

## 2026-09-26 阶段 7.1：复现与归因

- 用户授权按阶段 7 计划开始搭建并每步推送；工作分支 `codex/index-reliability`，基线 `6eae658`。
- 默认参数微型索引的写后/关闭重开/独立进程查询矩阵 12/12 通过；另一个实验确认 12 个未关闭客户端积累 24 个共享引用，关闭后回到基线。
- 定向 13 passed in 21.15s；全套基线 262 passed in 66.00s。没有复现历史读取异常，不宣称根因已解决。调查及修复范围见 [阶段 7 记录](docs/acceptance/2026-09-26-index-reliability.md)。
- 下一步补全生命周期；保留用户原有三份文件。

## 2026-09-26 进度归档与阶段 7 规划

- 进度总览已以阶段 6 为最新交付：6.1–6.4 完成，合并 `15d16e7`、归档 `3cbdc9b`，最近全量验收 249 passed；阶段 5 的 216 项结果保留为历史证据。
- 新增[阶段 7 详细计划](docs/superpowers/plans/2026-09-26-index-reliability-implementation.md)：复现归因、客户端生命周期、发布前查询验证、联合验收四步。依据为阶段 6 尚未归因的 Chroma 读取异常和当前发布校验范围。
- 阶段 7 为推荐规划，尚未实施。本轮仅整理文档、核对代码与交付记录，没有新增功能或重新运行产品测试。
- 技术发现补齐最新事实与未知项；README 增加文档导航。继续沿用每步提交推送、阶段结束合并的交付约定。

## 2026-09-26 阶段 6 主线合并

- 验收与使用说明以 `610248b` 独立推送后，阶段 6 已通过 `15d16e7` 合并并推送 GitHub `main`。
- 无合并冲突，合并树与已验收分支一致；最终回归 249 项通过，工作区只保留用户原有改动。
- 6.1–6.4 全部交付；进度总览、实施计划和验收记录已同步合并状态。

## 2026-09-26 索引维护 6.4：最终验收与文档

- 最终全量回归 **249 passed in 66.63s**；实际 `ragdb doctor` 退出码 0，隔离 CLI 与 Windows 原生桌面预览、确认清理、再次预览流程均通过。
- 客户端释放修正 `c068be9` 已推送。复核中已有重建用例曾发生一次 Chroma `Nothing found on disk`；该模块独立 15 项及最终全量通过，根因尚未完全确定，详见[验收记录](docs/acceptance/2026-09-26-vector-index-maintenance.md)。活动索引不可读时清理会拒绝执行。
- README 已补齐入口、预览确认、互斥、部分失败重试和回退限制；进度总览及实施计划更新为 6.1–6.4 完成。验收和复现临时目录已清理，用户已有三份文件 SHA256 保持不变。
- 本步骤独立提交推送后，按既有授权合并 GitHub `main`。

## 2026-09-26 索引维护验收修正

- 真实 CLI 子进程和 Windows 原生 Qt 界面均通过隔离合成数据的预览、确认与清理流程；各清理 4 项旧索引，当前 2 项活动索引保留，重新预览无候选。
- 首次完整回归暴露新增页签后的旧断言，以及大量测试 Chroma 库未释放导致的 Windows 文件句柄上限。维护服务现按操作关闭自身客户端，测试按用例释放自建 Chroma 系统；增加客户端引用不增长回归，更新第 4 个页签断言。
- 微型历史索引测试设置 3 条向量的持久化阈值，明确区分已持久化数据与首次写入尚未就绪的数据；产品对不可查询的活动索引仍拒绝清理。
- 修正后定向测试 50 passed in 14.62s；全量回归继续执行，尚不将首次失败计为通过。

## 2026-09-26 索引维护 6.3：确认清理与恢复

- 阶段 6.2 提交因自动审批额度不足曾中断；恢复后以 `3d976e4` 提交推送。现已接入 CLI `index clean-stale --preview-id ID --confirm` 和桌面确认清理。
- 清理持有跨进程独占门禁，核对预览、Chroma 身份、归属与活动索引；删除前验证活动索引存在、条数充足且可查询。失败时停止后续目标，显示已清理/未清理清单并允许重新预览重试；审计日志脱敏。
- 隔离数据定向回归：47 passed in 17.33s。覆盖真实 Chroma 删除、重建后检索/FTS 保持、陌生对象保护、预览过期、同名替换、导入/重建互斥、实际子进程遗留门禁恢复和部分故障重试。
- 曾复现 Chroma 新写入的 HNSW 索引尚未可读时发生首次读取错误，且发生在删除之前；产品增加活动可读性预检，测试在写入后先建立可读基线。临时复现目录已精确清理。下一步完成全量回归、原生桌面与 CLI 验收及使用说明。

## 2026-09-25 索引维护 6.2：预览入口

- 阶段 6.1 已以 `f898b6e` 推送。新增 `ragdb index list-stale` 和桌面“任务与诊断 → 索引维护”，呈现活动保护、旧索引候选、人工核查对象及向量条数。
- 预览不初始化缺少的 SQLite/Chroma 数据库，不读取模型密钥或调用模型；桌面后台操作防重复提交并保护窗口关闭。
- CLI、维护控件与既有模型设置页定向回归：22 passed in 4.53s。当前步骤仅预览，清理执行为 6.3。

## 2026-09-25 索引维护 6.1：安全盘点

- 已按用户授权开始阶段 6；新增只读 Chroma 清单与共享维护服务，验证名称、SQLite UUID、归属元数据与活动命名空间。
- 预览标识绑定存储路径、活动 profile、集合清单及 Chroma 身份与条数；同名对象重建后旧标识失效。不存在的向量目录不会被预览创建。
- 隔离数据定向回归：16 passed in 7.22s。并行代理因额度限制未完成，由主任务继续实现；真实知识库未执行清理。

## 2026-09-25 下一阶段计划成稿

- 已将[旧向量索引显式清理实施计划](docs/superpowers/plans/2026-09-25-vector-index-maintenance-implementation.md)拆为 6.1 安全盘点、6.2 预览入口、6.3 受保护清理、6.4 最终验收。明确活动索引、陌生对象和元数据不足的对象不能进入清理目标。
- 下一阶段仍处于规划状态；本轮只更新文档，没有执行实际清理或新增产品测试。

## 2026-09-25 进度归档与后续方向

- 项目一期八个里程碑、问答、学习产物、桌面工作台及模型设置均已交付到 GitHub `main`；最近一次产品代码验收为 216 passed、`ragdb doctor` 退出码 0，实际模型联调证据见[验收记录](docs/acceptance/2026-09-25-desktop-model-settings.md)。
- [进度总览](task_plan.md)已将模型设置 5.1–5.4 从“下一阶段”归入已完成事项。下一阶段聚焦原[模型设置设计](docs/superpowers/specs/2026-09-24-desktop-model-settings-design.md)明确延后的旧向量索引显式清理；目前为文档规划，尚无对应产品实现或新测试结果。
- 当前工作区的 `tests/integration/test_collection_cli.py` 修改和 `前端设计/` 草稿属于用户内容，保持原状且不纳入本轮文档提交。

## 2026-09-25 主线合并完成

- 按用户要求逐步推送：`023496a`（复核修正）、`cd7e169`（最终验收），随后以 `5efca40` 合并阶段 5 并推送 GitHub `main`。
- 合并无冲突，文件树与验收分支一致，216 项全量测试和实际 `ragdb doctor` 通过；本地与远端主线已同步。
- 阶段 1–5 均已完成并合并；更新进度总览、实施计划和验收记录作为最终交付留档。用户已有 CLI 测试文件和两份前端草稿内容保持不变。

## 2026-09-25 合并前最终验收

- 修正提交 `023496a` 已推送 GitHub。最终全量回归 **216 passed in 33.45s**，实际 `ragdb doctor` 退出码 0。
- 已核对计划与验收记录，完成本阶段全部约定内容；用户已授权合并主线。当前 GitHub 主线是功能分支祖先，无需处理代码冲突。
- 合并前确认用户已有文件内容未变、未纳入提交；本步骤更新进度总览、详细计划和验收证据后独立推送，再执行主线合并。

## 2026-09-25 合并前复核修正

- 用户已明确要求完成本阶段并合并主线；合并前既有全量回归 208 passed in 38.75s。
- 修正 `doctor` 的密钥优先级：显式空密钥不再被系统凭据库中的旧密钥掩盖。新增 4 个回归用例先复现失败，修复后通过。
- 增加旧版本地/云端活动 profile 的 SQLite 重开验证，覆盖 legacy 与指纹命名空间；修正 Ollama 无效响应测试，使非有限值和维度不一致实际到达对应校验分支。
- 修正后定向回归 23 passed in 2.21s。独立审查代理因额度限制未完成，由主任务接手检查；完整回归与主线合并继续执行。

## 最终交付摘要（2026-09-25）

- 阶段 5 已完成：本地 Ollama `embeddinggemma:latest` 嵌入、DeepSeek `deepseek-flash` 问答与学习生成均通过真实服务联调；系统凭据在重启后可恢复。
- 最终全量回归 216 passed，实际 `ragdb doctor` 退出码 0；隔离合成资料和临时目录已清理。所有完成步骤均独立提交推送，并通过 `5efca40` 合并进入 GitHub `main`。
- 验收结果见 [最终记录](docs/acceptance/2026-09-25-desktop-model-settings.md)；旧版进度快照保留在下方作为历史记录。

## 2026-09-25 系统凭据诊断修正

- 真实 DeepSeek 凭据已在系统凭据库安全保存，固定短文本连接测试通过；隔离合成集合的 Ollama 检索、DeepSeek 问答与学习摘要均通过，重启后配置和凭据可恢复。
- 实际执行 `ragdb doctor` 发现原诊断只检查环境变量，误报系统凭据缺失；现同时检查系统凭据库且不回显密钥。定向 20 passed，实际 doctor 退出码 0，最终全量回归 208 passed。
- 密钥未写入 `config.toml`、Git 提交或验收记录；端到端测试的临时目录已清理。

## 2026-09-25 DeepSeek 地址核验与项目配置

- 使用 gstack `/browse` 查阅 [DeepSeek 官方文档](https://api-docs.deepseek.com/)：`deepseek-flash` 为支持的模型名，OpenAI 兼容 Base URL 为 `https://api.deepseek.com`。
- 新增不含密钥的项目 `config.toml`，选择 Ollama `embeddinggemma:latest` 嵌入及 DeepSeek `deepseek-flash` 问答/学习生成；设置页服务地址提示不再误要求 `/v1`。
- 配置解析与桌面定向回归通过（24 passed）。云端凭据尚未进入本机环境，未发起真实 DeepSeek 请求。

## 2026-09-25 Ollama 嵌入扩展：步骤 3 真实验收

- 隔离合成集合中真实连接并切换到 Ollama `embeddinggemma:latest`，1 个切片重建成功，切换前后与重启后检索正常；临时数据已清理。
- 另一隔离合成集合中，未安装模型导致的真实服务错误、进度回调取消和随后重试均通过；旧索引在失败/取消后仍可检索，临时数据已清理。
- 增量全量回归 206 passed。用户指定聊天与学习生成使用 `deepseek-flash`；其服务 Base URL 和本机安全配置的凭据尚未确定，未发起云端请求。
- 结果与限制已写入 [验收记录](docs/acceptance/2026-09-25-desktop-model-settings.md)。

## 2026-09-25 Ollama 嵌入扩展：步骤 2 桌面配置

- 桌面嵌入表单新增“本地 Ollama”服务类型、模型名、服务地址、超时和已安装模型刷新；独立显示当前候选配置，继续经“重建并切换”发布。
- README 与配置示例补充 Ollama 操作步骤。定向设置页与服务测试 29 passed；下一步在隔离合成集合做真实重建与全量回归。

## 2026-09-25 Ollama 嵌入扩展：步骤 1 后端

- 按用户指定新增 Ollama 本地嵌入配置与 `/api/embed` 适配器，批量向量及维度校验接入现有嵌入工厂；旧本地与云端 profile 指纹保持兼容。
- 本机 `embeddinggemma:latest` 对固定合成文本的真实请求成功，返回 1 个 768 维向量；定向回归 37 passed。密钥未进入源码、日志或提交。
- 下一步将提供桌面设置入口并在隔离集合完成真实全局重建。

## 阶段 5 初期交付快照（2026-09-25）

- 阶段 1–4 已进入 `main`；阶段 5 工作分支 `codex/desktop-model-settings-acceptance`。文档 `7cea17f`、自动化验收 `ac159b6`、真实本地嵌入记录 `b093d6c` 已分别推送。
- 全量回归 198 passed；本地嵌入模型切换和检索真实通过。Ollama 无聊天模型，云端服务与测试凭据未配置，阶段 5 仍进行中。
- 验收矩阵与环境限制见 [验收记录](docs/acceptance/2026-09-25-desktop-model-settings.md)。

## 2026-09-25 模型设置阶段 5.3：真实服务联调（部分）

- 真实本地嵌入通过：隔离合成集合使用已缓存的 `BAAI/bge-small-zh-v1.5` 建索引，连接测试并重建切换至已缓存的 `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`；切换前后均检索到 1 条结果，测试目录已删除。
- Ollama 0.24.0 可运行，但只安装 `embeddinggemma:latest`，无聊天模型；云端服务、模型与测试凭据未配置。对应真实联调保持待验证，未发送云端请求或下载模型。状态矩阵见 [验收记录](docs/acceptance/2026-09-25-desktop-model-settings.md)。

## 2026-09-25 模型设置阶段 5.2：自动化验收

- 全量回归 198 passed，桌面设置定向回归 14 passed；CLI 帮助退出码 0，隔离数据目录的受控桌面启动退出码 0，Windows 原生模型设置页渲染成功。
- 修正 Qt 异步进度测试的等待时序，复核后未发现产品代码回归。完整命令、结果与环境限制见 [验收记录](docs/acceptance/2026-09-25-desktop-model-settings.md)。
- `uv run` 因沙箱缓存 ACL 和离线构建依赖获取失败；使用现有 `.venv` 运行同一测试集完成验收。

## 2026-09-25 模型设置阶段 5.1：使用指南

- 在 README 补充“系统 → 模型设置”的本地与云端配置步骤、连接测试、保存、全局嵌入重建、凭据与外部覆盖说明；纠正 `reindex` 只能处理集合本地文件的旧描述。
- 为 `config.example.toml` 与 `.env.example` 增加安全示例和两类云端密钥字段，未写入真实密钥。
- 验证：示例 TOML 可解析，`load_settings` 可加载；README 本地链接检查无缺失，`git diff --check` 通过。使用现有 `.venv` 运行检查；默认 uv 用户缓存受沙箱 ACL 限制。
- 当前 `main` 和 `origin/main` 已包含阶段 4；本阶段工作分支为 `codex/desktop-model-settings-acceptance`。保留用户既有 CLI 测试修改与“前端设计”目录。

## 历史交付摘要（2026-09-24）

- 模型设置阶段 1–4 完成；阶段 5“文档与最终验收”待执行。当前路线以 [task_plan.md](task_plan.md) 为入口。
- 阶段 4 提交 `a8240ae` 已推送至 `origin/codex/desktop-model-settings-ui`；尚未合并 `main`。阶段 1–3 已合并至主线 `f021716`。
- 最近一次完整回归：198 passed in 80.28s；Windows 原生 Qt 隐藏窗口渲染成功，已检查中文显示。
- 验证边界：自动化包含替身模型服务和故障注入；真实 Ollama、真实云端接口及真实系统凭据读写链路尚需最终联调确认。
- 本轮仅整理进度与下一阶段规划，未改动产品代码，未重新运行测试；保留下方历史记录。

文档分工：本文件保留交付证据；[实施计划](docs/superpowers/plans/2026-09-24-desktop-model-settings-implementation.md) 维护执行步骤和验收条件；[findings.md](findings.md) 保留技术结论。

## 2026-09-24 模型设置阶段 4

- 系统导航新增“模型设置”，分开配置问答/生成与嵌入模型；支持本地 Ollama、Sentence Transformers 和云端 OpenAI 兼容服务。
- 提供模型、地址、超时、批大小、掩码密钥、连接测试和 Ollama 已安装模型刷新；标注并锁定环境变量和 `.env` 接管字段。
- Chat 保存后更新运行时并可重启恢复；密钥仅保存在系统凭据库，按服务地址隔离，错误信息不回显服务响应或密钥。
- 嵌入候选配置显示当前模型、待应用模型和集合数；后台重建支持确认、进度、取消、失败重试和关闭窗口保护；云端发送资料前明确确认。
- 全量回归：`uv run pytest -q`，198 passed。包含目标服务变化后阻止未确认请求、密钥隔离、后台线程与取消保护。
- 按 frontend-design 的截图复核要求检查既有工作台布局：Qt offscreen 字体显示方框；改用 Windows 原生 Qt 平台（355 个字体族）后中文、字段和分区显示正常，无需修改产品字体。
- 原生隐藏窗口渲染成功生成 `model-settings-windows.png`；真实云端 API/Ollama 调用尚未实测，属于阶段 5 的可用环境验收范围。
- 本阶段提交推送分支为 `codex/desktop-model-settings-ui`；保留用户既有 CLI 测试修改和“前端设计”目录，不纳入提交。

## 2026-09-24 模型设置阶段 3

- 新增 `EmbeddingRebuildService`：直接读取 SQLite 当前代次切片，覆盖本地文件、网页、GitHub 和手动文本；分批生成新向量、校验持久化结果并汇报进度，不修改切片与 FTS。
- 完整构建后在同一 SQLite 事务中发布活动 profile 与来源模型信息；保留旧命名空间。嵌入、向量写入、校验、发布失败或取消时，清理暂存向量并维持旧索引可检索。
- schema v5 增加跨进程重建门禁与导入租约；导入、目录监听删除、集合和来源删除受门禁保护。旧服务实例在 profile 切换后拒绝继续写入，新建 runtime 服务读取最新 profile。
- Windows 使用 OpenProcess/GetExitCodeProcess 查询进程存活，替换不适用于该平台的 `os.kill(pid, 0)`；死进程留下的门禁/租约可恢复，重试先清理对应暂存空间。
- 云端候选凭据在发布前安全保存；发布后原子同步 TOML。同步失败返回明确警告，数据库保持权威，`from_config` 启动时重试恢复配置文件。
- 定向回归：50 passed。最终全量回归：`uv run pytest -q`，178 passed in 70.16s。新增测试覆盖四类来源、不同维度、当前代次过滤、FTS 保持、三类存储故障、末批次取消、进程探测、跨进程门禁及重启恢复。
- 本阶段按约定独立提交并推送 `codex/desktop-model-settings`；桌面设置页面仍属于下一阶段。

## 2026-09-24 模型设置阶段 1

- 新增 `ModelSettingsService`：TOML 原位保留其他配置与注释，系统凭据库存放云端 API Key；环境变量和 `.env` 仍优先，密钥不可用时不回退到明文文件。
- 新增 Chat/embedding 固定最小请求连接测试，以及 Ollama 已安装模型列表查询；模型测试不触碰知识库索引。
- 声明并锁定 `keyring`、`tomlkit` 依赖。
- 定向验证：`uv run pytest tests/unit/test_model_settings.py tests/unit/test_config.py tests/unit/chat/test_chat_models.py tests/unit/embeddings/test_openai_compatible.py`，18 passed；新增覆盖环境密钥优先和凭据删除。
- `/browse` 打开 TOMLKit 官方页面时遇到状态目录 ACL 启动超时；未进行其他网页浏览。keyring Windows 凭据库资料此前已由 `/browse` 查阅。
- 用户既有 `tests/integration/test_collection_cli.py` 修改与未跟踪 `前端设计/` 仍保持原样。

## 2026-09-24 模型设置阶段 2

- SQLite schema 升至 v4，新增单例活动嵌入 profile；JSON 仅记录非敏感配置，保存稳定 SHA-256 指纹与向量命名空间。
- `ApplicationRuntime` 初始化时以数据库 profile 为活动嵌入配置；旧数据库首次初始化继续指向旧版 Chroma 命名空间，避免既有向量失联。
- Chroma 集合名支持按 profile 指纹隔离；collection/source/ingestion/search runtime 路径共用当前 profile 命名空间。
- 云端嵌入凭据改按非敏感 profile 指纹区分，避免保存候选配置时覆盖旧活动 profile 的 key。
- 定向验证：模型配置、数据库迁移/仓储、Chroma 向量测试和配置测试共 49 passed。
- 全量验证：`uv run pytest -q`，163 passed in 22.94s。

## 2026-09-24 桌面端模型设置

- 用户批准模型设置设计，范围包括问答/生成模型、本地/云端嵌入模型、连接测试及所有集合的安全重建。
- 设计规格提交：`be70c06 docs: design desktop model settings`。
- 创建并推送功能分支：`codex/desktop-model-settings`。分支首推包含本地尚未上传的项目收尾提交与模型设置规格提交。
- 已建立五阶段实施计划；之后每阶段独立验证、提交并推送该分支。
- 依赖分析后调整阶段顺序：先完成活动嵌入命名空间和全集合安全重建，再向用户开放可保存的嵌入模型设置。

## 2026-09-24 项目进度核验与执行

- 确认核心路线、问答、学习产物和桌面工作台均已完成；开始发布前健康检查。
- 工作区保护：不改动既有集合 CLI 测试修改或未跟踪的“前端设计”草稿。
- 全量验证：`uv run pytest -q`，152 passed in 24.50s。
- CLI 验证：`uv run ragdb --help` 成功；所有既有命令组均可发现。
- 未发现项目代码回归；本次仅更新项目跟踪文档，没有产品代码改动。

## 2026-09-24 项目收尾

- 将 2026-09-16 主实施计划中的八个里程碑状态更新为最终完成情况，并注明后续问答、学习产物与桌面工作台进展。
- 移除 CLI 中没有调用点的 `_pending()` 占位辅助函数及 `NOT_IMPLEMENTED` 退出码。
- 保留既有 `tests/integration/test_collection_cli.py` 工作区状态和未跟踪 `前端设计/` 目录。
- 按当前指令未运行测试；改动仅涉及状态文档与无调用点代码清理。

## 2026-09-23

- 审查已完成的桌面工作台与 UI 重构；确认当前主线已与 `origin/main` 对齐，工作区存在用户保留的测试改动及“前端设计”目录。
- 在用户确认的范围内完成完整回归：`uv run pytest`，`144 passed in 19.63s`。
- 用户确认以提供的侧栏交互语言为基准，并批准导航布局、操作反馈、键盘可访问性三阶段方案。
- 设计规格已提交：`098922c docs: design desktop experience polish`。
- 已建立详细实施计划。
- 步骤 1 完成：侧栏宽度、折叠和详情栏偏好均已安全持久化；支持边缘拖拽调宽、减少动效以及窄窗临时收起和恢复。
- `uv run pytest tests/unit/test_desktop.py` 通过（12 passed）。首次沙箱执行无法访问用户级 uv 缓存，经授权重试后通过。
- 第一阶段本地提交：`35523de feat: persist desktop layout preferences`。推送 `origin/main` 被安全策略拒绝，因为直接修改共享默认分支需要用户明确确认；未重试。
- 步骤 2 完成：检索、问答和学习产物提交会防止重复请求、在后台任务结束后恢复入口并展示结果状态；集合导入也采用相同的防重复与恢复规则。
- `uv run pytest tests/unit/test_desktop.py` 通过（14 passed），覆盖成功、失败和重复提交防护。
- 步骤 3 完成：增加页面切换、侧栏切换与 Escape 快捷键；调整手柄支持键盘；导航、详情栏和主要动作补足可访问名称及焦点样式。
- `uv run pytest tests/unit/test_desktop.py` 通过（17 passed），覆盖键盘手柄、快捷键、Escape 和多行编辑器焦点保护。
- 验收完成：`uv run pytest` 通过（152 passed in 21.49s）；受控无头 `ragdb-gui` 启动检查正常退出。

---

# Historical Progress Log: 网页与公开 GitHub 仓库导入

## 2026-09-20

- 审阅当前实施计划、最近提交和搜索链路。
- 用户确认：先完成规划，再实施；每个完成步骤均提交并推送 GitHub。
- 用户确认：多个 `--tag` 必须同时匹配。
- 用户确认了元数据契约与索引/错误处理设计。
- 已提交并推送设计文档：`0657573 docs: design search metadata filters`。
- 用户已审阅并确认设计文档。
- 已写入三步实施计划；下一步为提交并推送该计划，然后实施步骤 1。
- 步骤 1 已完成并推送：`47a148b feat: add controlled ingestion metadata`；定向测试 14 passed。
- 步骤 2 已完成并推送：`63b509e feat: filter retrieval by controlled metadata`；后端集成测试 17 passed。
- 步骤 3 已完成并推送：`06f923b feat: expose metadata search filters`；全量测试 93 passed。

## 2026-09-21

- 审查项目主计划、当前实现、Git 历史与工作树：第六里程碑（网页与公开 GitHub 导入）是下一项未完成范围。
- 用户确认严格按既有里程碑顺序继续，并批准独立受限适配器方案。
- 已写入网页与公开 GitHub 仓库导入设计规格，待提交并推送后请用户审阅。
- 设计规格已提交并推送：`5930141 docs: design web and GitHub import`。
- 用户已审阅并确认规格；已写入三步实施计划，待提交并推送后开始步骤 1。
- 实施计划已提交并推送：`3505866 docs: plan web and GitHub import`；开始步骤 1。
- `uv` 首次检查因沙箱禁止访问用户缓存失败，待授权后更新 HTTPX 与 Trafilatura 锁定依赖。
- 已在授权后锁定并安装 HTTPX、Trafilatura；`uv.lock` 已更新。
- 步骤 1 已完成：新增受限网页抓取、robots 策略和正文提取模块，以及 6 项定向测试；`uv run pytest tests/unit/web` 通过（6 passed），待提交推送。
- 步骤 1 已推送：`34b4ade feat: add bounded web crawler`。
- 步骤 2 已完成：网页正文可作为 `web` 资料以 URL 为稳定标识导入、更新或跳过，`ragdb crawl` 已替换占位命令；定向测试 13 passed，待提交推送。
- 步骤 2 已推送：`80f7574 feat: import crawled web pages`。
- 步骤 3 已完成：`ragdb repo` 支持经校验的公开 GitHub HTTPS 仓库浅克隆与受控文件过滤；定向测试 9 passed，待提交推送。
- 步骤 3 已推送：`a39f32e feat: import public GitHub repositories`。
- 第六里程碑验收：`uv run pytest` 通过（104 passed）；`uv run python -m ragdb crawl --help` 与 `repo --help` 通过，待提交验收记录。
- 用户批准前台持续运行的目录监听与显式集合重建方案；正在写入设计规格。
- 第七里程碑验收：`uv run pytest` 通过（109 passed）；`watch --help` 与 `reindex --help` 通过。
